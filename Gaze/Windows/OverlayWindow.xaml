<Window x:Class="Gaze.Windows.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Gaze.Controls"
        WindowStyle="None" AllowsTransparency="True" 
        Background="Transparent" Topmost="True"
        ShowInTaskbar="False" ResizeMode="NoResize"
        Title="Gaze Overlay">
    <Grid>
        <!-- Detection zone: 20px tall, nearly invisible but hit-testable -->
        <Border x:Name="DetectionZone" 
                Background="#01000000"
                VerticalAlignment="Top" HorizontalAlignment="Center"
                Height="20" Width="200"
                MouseEnter="DetectionZone_MouseEnter"/>
        
        <!-- The Dynamic Island -->
        <local:IslandControl x:Name="Island"
                             HorizontalAlignment="Center" VerticalAlignment="Top"/>
    </Grid>
</Window>
